%% 8.25 rac158
% No code is written for this assignment, it's in its own file for continuity.

function validVector()

% kit(1).sub:
%   id: 33
%   wt: 11.11
%   code: 'q'
% 
% kit(2).lens(1)
%   4
% 
% kit(1).code
%   invalid, the `kit` struct doesn't contain a `code` field, the nested `sub` struct does.
% 
% kit(2).sub.id == kit(1).sub.id
%   0 [false]
% 
% strfind(kit(1).name, 's')
%   []
% 

end
